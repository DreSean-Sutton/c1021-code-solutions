/* exported isUpperCased */
/*  if the word fully uppercase
return true
otherwise,
return false
*/
function isUpperCased(word) {
  if (word === word.toUpperCase()) {
    return true;
  } else {
    return false;
  }
}
