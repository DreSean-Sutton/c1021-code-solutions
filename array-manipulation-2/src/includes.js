/* exported includes */
/*

*/

function includes(array, value) {
  var yesOrNo = Boolean;
  for (var i = 0; i < array.length; i++) {
    if (array[i] === value) {
      yesOrNo.push;
    } else {
      return false;
    }
  }
}
